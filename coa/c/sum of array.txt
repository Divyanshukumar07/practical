#include<stdio.h>
//#include<conio.h>
int main()

{
    int n,i,c=0;
    printf("enter the number of elements: ");
    scanf("%d",&n);
    int a[n];

    for(i=0;i<n;i++)
    {
        printf("enter the numbers: ");
        scanf("%d",&a[i]);
        c=c+a[i];
    }
    printf("sum of array:%d",c);
    return 0;
}