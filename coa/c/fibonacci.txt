# include <stdio.h>
int main() {
    int n, a = 0, b = 1, c;
    printf(" Enter no of terms");
    scanf("%d", &n);
    while (n > 0) {
        printf("%d", a);
        c = a + b;
        a = b;
        b = c;
        n--;
    }
    return 0;
}